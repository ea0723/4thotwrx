class Conversion < ActiveRecord::Base
    
    # validates_exclusion_of ("glob glob glob glob" || "prok prok" ||"pish pish pish pish" || "tegj tegj"), :in => :convert_me
    # validates_presence_of :convert_me
    
end
